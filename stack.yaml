resolver: lts-22.21

packages:
  - .

ghc-options:
  "$locals": -Wall

allow-newer: false

extra-deps: []

flags:
  hashtables:
    portable: true
    sse42: false
    unsafe-tricks: false
  warp:
    x509: false
